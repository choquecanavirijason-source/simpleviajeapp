// ⚙️ Configuración global de la app

// (modo test) = true
// (modo produccion) = false
class AppConfig {
  static const bool skipOtpSend = false; // Recibir OTP al telefono
  static const bool skipOtpVerification = false;

  /// verificacion OTP
}
