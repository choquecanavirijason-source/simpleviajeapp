/// Radios de borde del estilo "Modern Clean Light UI".
class AppRadius {
  /// rounded-3xl (24px): tarjetas.
  static const double card = 24;

  /// Píldora: badges y bottom nav flotante.
  static const double pill = 999;
}
